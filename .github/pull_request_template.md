<!-- A short summary of what changed and why is enough. -->

Before opening: `python3 scripts/validate_plugin.py` passes, the version in `plugins/vibes/.claude-plugin/plugin.json` is bumped, and `CHANGELOG.md` has a line. See CONTRIBUTING.md.
